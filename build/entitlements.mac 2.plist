<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened Runtime entitlements for Developer ID + notarization.
       NOT App Sandbox (com.apple.security.app-sandbox is deliberately
       absent) — this is a direct-download build, not Mac App Store, so
       screen sharing via desktopCapturer keeps working. -->

  <!-- Electron / V8 need JIT and writable+executable memory. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- The bundled whisper-cli helper binary is a separate Mach-O loaded
       at runtime; library validation must be relaxed so it can run. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Camera + microphone for video calls (also gated by the
       NS*UsageDescription strings in Info.plist via extendInfo). -->
  <key>com.apple.security.device.camera</key>
  <true/>
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
